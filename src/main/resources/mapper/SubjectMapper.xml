<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.secondproject.teacher.subject.SubjectMapper">


    <select id="sbjCate" resultType="SubjectVo">
        SELECT nm FROM sbj_category
        WHERE TYPE = 1
    </select>

    <select id="subDetail">
        SELECT * FROM subject A
        INNER JOIN sbj_category B
        ON A.category_id = B.category_id
        WHERE A.category_id = #{category_id} AND TYPE =1
    </select>
</mapper>