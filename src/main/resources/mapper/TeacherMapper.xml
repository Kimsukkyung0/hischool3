<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.secondproject.teacher.TeacherMapper">
    
    <select id="selSignedStudent">
        SELECT class_id classId, apr_yn aprYn, nm snm, birth, phone, email
        FROM user
        WHERE role = "ROLE_STD"
            AND apr_yn = 1
            AND class_id = #{classId}
    </select>


    <select id="selUnsignedStudent">
        SELECT class_id classId, apr_yn aprYn, nm snm, birth, phone, email
        FROM user
        WHERE role = "ROLE_STD"
        AND apr_yn = 0
        AND class_id = #{classId}
    </select>
    


    <select id="selAcaResult">
        SELECT year, semester, mid_final mf, score, rating
                , class_rank cr, whole_rank wr
        FROM aca_result
        WHERE user_id = #{userId}
    </select>


    <select id="selMockResult">
        SELECT year, mon, standard_score sc, rating, percent
        FROM mock_result
        WHERE user_id = #{userId}
    </select>



    <update id="delTeacher">
        UPDATE user
        SET del_yn = 1
        WHERE user_id = #{userId}
        AND role = "ROLE_TC"
    </update>

</mapper>
